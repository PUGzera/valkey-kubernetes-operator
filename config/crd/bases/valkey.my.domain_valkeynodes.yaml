---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: valkeynodes.valkey.my.domain
spec:
  group: valkey.my.domain
  names:
    kind: ValkeyNode
    listKind: ValkeyNodeList
    plural: valkeynodes
    singular: valkeynode
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ValkeyNode is the Schema for the valkeynodes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ValkeyNodeSpec defines the desired state of ValkeyNode
            properties:
              replicas:
                description: Foo is an example field of ValkeyNode. Edit valkeynode_types.go
                  to remove/update
                type: integer
            type: object
          status:
            description: ValkeyNodeStatus defines the observed state of ValkeyNode
            properties:
              nodeState:
                properties:
                  deployment:
                    type: string
                  master:
                    properties:
                      IP:
                        type: string
                      pod:
                        type: string
                    required:
                    - IP
                    - pod
                    type: object
                  replicas:
                    items:
                      properties:
                        IP:
                          type: string
                        pod:
                          type: string
                      required:
                      - IP
                      - pod
                      type: object
                    type: array
                required:
                - deployment
                - master
                - replicas
                type: object
              observedGeneration:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                format: int64
                type: integer
            required:
            - nodeState
            - observedGeneration
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
